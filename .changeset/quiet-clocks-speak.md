---
"marginalia": patch
---

Show when each chat message was sent. Hovering a message now floats its full date and time just above the bubble — the spot the assistant's name used to occupy, which is gone now — and the conversation gets a "Today" / "Yesterday" / date divider wherever it crosses into a new day, so scrolling back through a long-lived conversation reads as a record instead of a flat stream.
