---
"marginalia": minor
---

Add a configurable reasoning effort (Off / Low / Medium / High) for the chat and summary models in Settings → Models. Leave it on Default to use the provider's own default. Off disables reasoning where the model supports it.
