---
"marginalia": patch
---

Add a configurable global concurrency cap for background AI tasks (chapter/book summaries, conversation naming, and long-conversation compaction). Set it in Settings → Advanced (default 3); excess tasks queue and run as slots free up. Foreground chat replies are never throttled.
