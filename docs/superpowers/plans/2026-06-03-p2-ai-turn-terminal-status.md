# P2 · AI 终态模型 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 `messages` 加终态 `status` 列，把失败/中止轮**显式补落**为带终态的 assistant 消息（含 `error{name,message}` 与 token `usage`），消除「孤儿 user turn」歧义；进程硬崩溃的未完成轮靠「尾消息是 user 行」**读时派生**识别。

**Architecture:** `messages.status ∈ {complete,error,aborted}`（出生即终态、写一次不再改，符合 DB lifecycle spec §0）。`MessageMetadata` 扩 `usage`（改成真填，从 `streamText` 的 `onFinish.totalUsage` 捕获）与 `error`（`{name,message}`，透传 provider 真实值）。`send.ts` 的 `toUIMessageStream.onFinish` 不再用 `!isAborted && !streamHadError` 双守卫跳过，而是按 `streamHadError ? "error" : isAborted ? "aborted" : "complete"` 计算终态并落库。崩溃态不持久化，由 `isLastTurnIncomplete` 派生。

**Tech Stack:** TypeScript 6、Drizzle 1.0-rc（better-sqlite3）、Vitest 4、Vercel AI SDK v6（`streamText`/`toUIMessageStream`）。设计依据：`docs/superpowers/specs/2026-06-03-db-lifecycle-rules-design.md` §3 / DD-§3.1 / DD-§3.2。

**AI SDK 事实（已核 `node_modules/ai/dist/index.d.ts`）：**

- `streamText` 的 `onFinish` 收 `OnFinishEvent`，含 `totalUsage: LanguageModelUsage`（跨步聚合）。
- `LanguageModelUsage = { inputTokens: number | undefined; outputTokens: number | undefined; … }`（**扁平**）。
- `result.toUIMessageStream({ onFinish })` 的 `onFinish` 收 `{ responseMessage, isAborted, … }`——**不带 usage**，故 usage 必须从 `streamText.onFinish` 旁路捕获。
- `LanguageModelUsage` 由 `"ai"` 导出，可 `import type`。

**渲染层影响（RA 轨，不在本计划）：** renderer 历史回放需按 `status` 渲染「失败+重试 / 已中止 / 未完成+重试」样式（用 `MessageDto.status` 与 `isLastTurnIncomplete`）。本计划仅落主进程数据与派生，不动 UI。

---

## File Structure

| 文件                             | 责任                                                        | Task |
| -------------------------------- | ----------------------------------------------------------- | ---- |
| `src/shared/types.ts`            | 加 `MessageStatus` 类型；`messageMetadataSchema` 加 `error` | 1    |
| `src/main/db/schema.ts`          | `messages` 加 `status` 列 + CHECK                           | 1    |
| `src/shared/chat.ts`             | `MessageDto` 加 `status`                                    | 1    |
| `src/main/chat/messages.ts`      | `AppendMessageInput`/`toDto`/insert 接 `status`             | 1    |
| `src/main/db/migrations/<new>/`  | `pnpm db:generate` 生成的加列迁移（表重建）                 | 1    |
| `src/main/chat/messages.test.ts` | status 默认/透传/error 往返 + `isLastTurnIncomplete`        | 1, 3 |
| `src/main/ai/send.ts`            | onFinish 改为补落终态 + usage + error                       | 2    |
| `src/main/ai/send.test.ts`       | error/abort/complete 终态 + usage 断言                      | 2    |
| `src/main/chat/messages.ts`      | 加 `isLastTurnIncomplete` 派生 helper                       | 3    |

> **Refactor 性质：** Task 1 是数据模型扩展（加列 + 类型，向后兼容），Task 2 改写 send.ts 落库语义，Task 3 加派生 helper。三 Task 各自编译通过、测试全绿、独立提交。

---

## Task 1: `messages.status` 终态列 + metadata.error + DTO 接线

**Files:**

- Modify: `src/shared/types.ts`
- Modify: `src/main/db/schema.ts`
- Modify: `src/shared/chat.ts`
- Modify: `src/main/chat/messages.ts`
- Test: `src/main/chat/messages.test.ts`
- Create: `src/main/db/migrations/<auto-generated>/`

- [ ] **Step 1: `shared/types.ts` 加 `MessageStatus` + metadata.error**

在 `MessageRole` 旁（当前第 10 行后）加：

```ts
/** 一轮 AI 对话的终态（仅 assistant 行可能非 complete；写一次不再改，见 DB lifecycle spec §3）。 */
export type MessageStatus = "complete" | "error" | "aborted";
```

`messageMetadataSchema`（当前第 22–39 行）在 `usage` 之后加 `error` 字段：

```ts
export const messageMetadataSchema = z.object({
  contextChips: z
    .array(
      z.object({
        id: chipIdSchema,
        content: z.string(),
        tokenCount: z.number().int().nonnegative(),
      }),
    )
    .optional(),
  model: z.string().optional(),
  usage: z
    .object({
      inputTokens: z.number().int().nonnegative(),
      outputTokens: z.number().int().nonnegative(),
    })
    .optional(),
  // error 终态的原因（透传 provider 真实 name/message；不编造、不分类——结构化 reason 是 ma5-deferred #6）。
  error: z
    .object({
      name: z.string(),
      message: z.string(),
    })
    .optional(),
});
```

- [ ] **Step 2: `schema.ts` 给 `messages` 加 `status` 列 + CHECK**

`src/main/db/schema.ts` 的 `messages` 表（当前第 149–167 行）：在 `metadata` 列之后、`seq` 之前加 `status` 列：

```ts
    metadata: text("metadata", { mode: "json" }).$type<MessageMetadata>(),
    status: text("status", { enum: ["complete", "error", "aborted"] })
      .notNull()
      .default("complete"),
    seq: integer("seq").notNull(),
```

并在该表的约束数组（当前第 162–166 行）补 CHECK：

```ts
  (t) => [
    check("messages_role_check", sql`${t.role} in ('system','user','assistant')`),
    check("messages_status_check", sql`${t.status} in ('complete','error','aborted')`),
    unique("messages_conversation_seq_unique").on(t.conversationId, t.seq),
    index("messages_conversation_id_idx").on(t.conversationId),
  ],
```

- [ ] **Step 3: `shared/chat.ts` 给 `MessageDto` 加 `status`**

`src/shared/chat.ts`：导入（当前第 5 行）加入 `MessageStatus`：

```ts
import type { MessageMetadata, MessageRole, MessageStatus } from "@shared/types";
```

`MessageDto`（当前第 62–70 行）加 `status`：

```ts
export interface MessageDto {
  id: string;
  conversationId: string;
  role: MessageRole;
  parts: UIMessage["parts"];
  metadata: MessageMetadata | null;
  status: MessageStatus;
  seq: number;
  createdAt: number;
}
```

- [ ] **Step 4: `messages.ts` 的 `AppendMessageInput`/`toDto`/insert 接 `status`**

`src/main/chat/messages.ts`：导入（当前第 7 行）加 `MessageStatus`：

```ts
import {
  messageMetadataSchema,
  type MessageMetadata,
  type MessageRole,
  type MessageStatus,
} from "@shared/types";
```

`toDto`（当前第 20–30 行）加 `status`：

```ts
function toDto(row: MessageRow): MessageDto {
  return {
    id: row.id,
    conversationId: row.conversationId,
    role: row.role,
    parts: row.parts,
    metadata: parseMetadata(row.metadata),
    status: row.status,
    seq: row.seq,
    createdAt: row.createdAt,
  };
}
```

`AppendMessageInput`（当前第 32–37 行）加可选 `status`：

```ts
export interface AppendMessageInput {
  conversationId: string;
  role: MessageRole;
  parts: UIMessage["parts"];
  metadata?: MessageMetadata | null;
  /** 终态；省略默认 complete（user/system 行恒 complete）。 */
  status?: MessageStatus;
}
```

`appendMessage` 的 insert `.values`（当前第 52–58 行）加 `status`：

```ts
      .values({
        conversationId: input.conversationId,
        role: input.role,
        parts: input.parts,
        metadata: input.metadata ?? null,
        status: input.status ?? "complete",
        seq: nextSeq,
      })
```

- [ ] **Step 5: `messages.test.ts` 加 status 用例**

`src/main/chat/messages.test.ts`：在 `describe("appendMessage / listMessages", …)` 内末尾（当前第 117 行 `});` 之前）加两个用例：

```ts
it("defaults status to 'complete' when not provided", () => {
  const db = freshDb();
  const cid = seedConversation(db);
  appendMessage(db, { conversationId: cid, role: "user", parts: [{ type: "text", text: "hi" }] });
  expect(listMessages(db, cid)[0].status).toBe("complete");
});

it("persists an explicit terminal status and error metadata", () => {
  const db = freshDb();
  const cid = seedConversation(db);
  appendMessage(db, {
    conversationId: cid,
    role: "assistant",
    parts: [],
    status: "error",
    metadata: { error: { name: "AI_APICallError", message: "quota exceeded" } },
  });
  const [msg] = listMessages(db, cid);
  expect(msg.status).toBe("error");
  expect(msg.metadata?.error).toEqual({ name: "AI_APICallError", message: "quota exceeded" });
});
```

- [ ] **Step 6: 生成加列迁移**

Run: `pnpm db:generate`
Expected: 新增一个迁移子目录。因新增的是**表级 CHECK** 约束（SQLite 不能 `ALTER … ADD CHECK`），drizzle-kit 生成 `messages` 的**表重建**（`CREATE __new_messages`（含 `status` + 两个 CHECK）→ `INSERT…SELECT`（旧行 status 取默认 `'complete'`）→ `DROP messages` → `RENAME`）。`runMigrations`（`client.ts:28`）事务外 `PRAGMA foreign_keys=OFF` 使重建安全（`messages` 引用 `conversations`）。

- [ ] **Step 7: 跑测试 + 类型检查**

Run: `pnpm test src/main/chat/messages.test.ts`
Expected: PASS（新两用例 + 原有用例全绿）

Run: `pnpm typecheck`
Expected: 无错误（`MessageDto.status` 已由 `toDto` 提供；现有 `appendMessage` 调用未传 `status` 合法，默认 complete）

- [ ] **Step 8: 提交**

```bash
git add src/shared/types.ts src/main/db/schema.ts src/shared/chat.ts src/main/chat/messages.ts src/main/chat/messages.test.ts src/main/db/migrations/
git commit -m "feat(messages): add terminal status column + error metadata (#9 P2)

messages.status ∈ {complete,error,aborted}（默认 complete）+ CHECK；
MessageMetadata 加 error{name,message}；MessageDto/appendMessage 接 status。
为 send.ts 终态落库铺路。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: `send.ts` 补落终态 assistant 消息 + usage + error

**Files:**

- Modify: `src/main/ai/send.ts`
- Test: `src/main/ai/send.test.ts`

- [ ] **Step 1: `send.ts` 导入 `LanguageModelUsage`**

`src/main/ai/send.ts` 第 2 行：

```ts
import { stepCountIs, streamText, type ModelMessage, type UIMessageChunk } from "ai";
```

替换为：

```ts
import {
  stepCountIs,
  streamText,
  type LanguageModelUsage,
  type ModelMessage,
  type UIMessageChunk,
} from "ai";
```

- [ ] **Step 2: `send.ts` 改写 streamText 调用 + onFinish 落库**

把 `src/main/ai/send.ts` 的「步骤 8 streamText」与「步骤 9 onFinish」段（当前第 115–155 行，从 `// 8. streamText + tools + agent 循环` 到 `});` 即 `toUIMessageStream` 结束）整体替换为：

```ts
// 8. streamText + tools + agent 循环
const tools = createReadingTools({ db, bookId: input.bookId, loadBytes });
// 从 streamText 自身的 onFinish 旁路捕获跨步聚合用量（toUIMessageStream 的 onFinish 不带 usage）。
let capturedUsage: LanguageModelUsage | undefined;
const result = streamText({
  model: resolved.model,
  system: systemPrompt,
  messages,
  tools,
  stopWhen: stepCountIs(stepLimit ?? 5),
  abortSignal: opts?.abortSignal,
  onFinish: ({ totalUsage }) => {
    capturedUsage = totalUsage;
  },
});

// 9. 一轮终止时落 assistant 消息——出生即终态（complete|error|aborted），设计文档 §3 / DD-§3.1 / DD-§3.2。
let resolveDone!: () => void;
const finished = new Promise<void>((res) => {
  resolveDone = res;
});

// streamText 遇 doStream 错误时发 error chunk 并正常关流（isAborted 仍 false），故用 streamHadError 标志区分；
// 用户中止经 abortSignal → onFinish 的 isAborted=true（SDK 权威信号，非嗅探 AbortError，DD-§3.2）。
// 两种终止都补落带终态的 assistant（不再用双守卫跳过 → 不再产生孤儿 user turn）。
let streamHadError = false;
let errorInfo: { name: string; message: string } | undefined;
const uiStream = result.toUIMessageStream({
  onError: (err) => {
    streamHadError = true;
    errorInfo = {
      name: err instanceof Error ? err.name : "Error",
      message: err instanceof Error ? err.message : String(err),
    };
    console.warn("[send] stream/model error:", err);
    // onError 要求返回 string（作为 error chunk 的 errorText，供 renderer 实时显示）
    return errorInfo.message;
  },
  onFinish: ({ responseMessage, isAborted }) => {
    const status = streamHadError ? "error" : isAborted ? "aborted" : "complete";
    const usage =
      capturedUsage?.inputTokens != null && capturedUsage.outputTokens != null
        ? { inputTokens: capturedUsage.inputTokens, outputTokens: capturedUsage.outputTokens }
        : undefined;
    appendMessage(db, {
      conversationId,
      role: "assistant",
      parts: responseMessage.parts, // 完整 / 中止前的 partial / 报错前已流出
      status,
      metadata: {
        model: resolved.modelId,
        usage,
        error: streamHadError ? errorInfo : undefined,
      },
    });
  },
});
```

（其后的 `tee` / drain IIFE（当前第 157–176 行）与 `return` 保持不动。）

- [ ] **Step 3: `send.test.ts` 把「错误不落库」用例改为「补落 error 终态」**

`src/main/ai/send.test.ts` 的 `it("does not persist an assistant message when streaming errors", …)`（当前第 166–179 行）整体替换为：

```ts
it("persists an error-status assistant message when streaming errors", async () => {
  const failModel = new MockLanguageModelV3({
    doStream: async () => {
      throw new Error("stream boom");
    },
  });
  const { db, book, ch1, deps } = setup({ ok: true, model: failModel, modelId: "mock" });
  const r = runSend(deps, input(book.id, ch1.id));
  expect(r.ok).toBe(true);
  if (!r.ok) return;
  await r.finished;
  const msgs = listMessages(db, r.conversationId);
  expect(msgs.map((m) => m.role)).toEqual(["user", "assistant"]); // 不再是孤儿 user turn
  const assistant = msgs[1];
  expect(assistant.status).toBe("error");
  expect(assistant.metadata?.error?.message).toContain("stream boom");
});
```

- [ ] **Step 4: `send.test.ts` 给 happy-path 用例补 status/usage 断言**

在 `it("persists the user message with a chip snapshot and the streamed assistant message", …)` 里，`expect(r.created).toBe(true);`（当前第 145 行）之前插入：

```ts
expect(msgs[1].status).toBe("complete");
expect(msgs[1].metadata?.usage).toEqual({ inputTokens: 1, outputTokens: 1 });
```

（mock 的 `finishChunk` 携带 `USAGE`，`streamText` 聚合后 `totalUsage` 为扁平 `{ inputTokens: 1, outputTokens: 1 }`。）

- [ ] **Step 5: `send.test.ts` 加中止终态用例**

在 `it("forwards a non-aborted signal …", …)` 之后（当前第 226 行 `});` 之后）加：

```ts
it("persists an aborted-status assistant message when the signal aborts mid-stream", async () => {
  const controller = new AbortController();
  // 延迟分片：abort 在分片尚未发完时触发 → onFinish 收 isAborted=true。
  const slowModel = new MockLanguageModelV3({
    doStream: async () => ({
      stream: simulateReadableStream({
        chunkDelayInMs: 50,
        chunks: [
          { type: "text-start", id: "t1" },
          { type: "text-delta", id: "t1", delta: "partial" },
          { type: "text-end", id: "t1" },
          finishChunk("stop"),
        ],
      }),
    }),
  });
  const { db, book, ch1, deps } = setup({ ok: true, model: slowModel, modelId: "mock" });
  const r = runSend(deps, input(book.id, ch1.id), { abortSignal: controller.signal });
  expect(r.ok).toBe(true);
  if (!r.ok) return;
  controller.abort(); // runSend 同步返回后立即中止，分片仍在 50ms 延迟途中
  await r.finished;
  const assistant = listMessages(db, r.conversationId).find((m) => m.role === "assistant");
  expect(assistant?.status).toBe("aborted");
});
```

> 实施提示：`simulateReadableStream` 支持 `chunkDelayInMs`（已在本测试文件 `import` 自 `ai/test`）。若该用例因调度时序偶发 `complete`（abort 晚于流自然结束），调大 `chunkDelayInMs`（如 200）以扩大中止窗口。

- [ ] **Step 6: 跑测试 + 类型检查**

Run: `pnpm test src/main/ai/send.test.ts`
Expected: PASS（错误→error、正常→complete+usage、中止→aborted 三终态用例全绿；原有用例不受影响）

Run: `pnpm typecheck`
Expected: 无错误

- [ ] **Step 7: 提交**

```bash
git add src/main/ai/send.ts src/main/ai/send.test.ts
git commit -m "feat(send): persist terminal-status assistant message on error/abort (#9 P2)

替换 onFinish 双守卫跳过逻辑：失败→status=error+error{name,message}、
中止→status=aborted+partial、正常→complete；usage 从 streamText
onFinish.totalUsage 捕获落 metadata。消除孤儿 user turn。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: `isLastTurnIncomplete` 崩溃恢复派生 helper

**Files:**

- Modify: `src/main/chat/messages.ts`
- Test: `src/main/chat/messages.test.ts`

- [ ] **Step 1: `messages.ts` 加派生 helper**

`src/main/chat/messages.ts` 末尾（`getLastParagraphContent` 之后）加：

```ts
/**
 * 崩溃恢复派生（DD-§3.1）：会话尾消息是 user 行（其后无 assistant）即「未获回复的未完成轮」。
 * 进程硬崩溃流到一半时不落 assistant，故只靠此读时派生识别——无需持久化任何运行态。
 */
export function isLastTurnIncomplete(db: DB, conversationId: string): boolean {
  const last = db
    .select({ role: messages.role })
    .from(messages)
    .where(eq(messages.conversationId, conversationId))
    .orderBy(desc(messages.seq))
    .limit(1)
    .get();
  return last?.role === "user";
}
```

（`desc`/`eq` 已在文件第 2 行 import；`.limit()` 是查询构造器方法，无需额外 import。）

- [ ] **Step 2: `messages.test.ts` 加 helper 用例**

`src/main/chat/messages.test.ts` 导入（当前第 7 行）加 `isLastTurnIncomplete`：

```ts
import {
  appendMessage,
  getLastParagraphContent,
  isLastTurnIncomplete,
  listMessages,
} from "@main/chat/messages";
```

文件末尾（最后一个 `});` 之后）加：

```ts
describe("isLastTurnIncomplete", () => {
  it("returns false for an empty conversation", () => {
    const db = freshDb();
    const cid = seedConversation(db);
    expect(isLastTurnIncomplete(db, cid)).toBe(false);
  });

  it("returns true when the last message is a user turn (crash before assistant reply)", () => {
    const db = freshDb();
    const cid = seedConversation(db);
    appendMessage(db, { conversationId: cid, role: "user", parts: [{ type: "text", text: "q" }] });
    expect(isLastTurnIncomplete(db, cid)).toBe(true);
  });

  it("returns false when an assistant reply follows the user turn", () => {
    const db = freshDb();
    const cid = seedConversation(db);
    appendMessage(db, { conversationId: cid, role: "user", parts: [{ type: "text", text: "q" }] });
    appendMessage(db, {
      conversationId: cid,
      role: "assistant",
      parts: [{ type: "text", text: "a" }],
      status: "complete",
    });
    expect(isLastTurnIncomplete(db, cid)).toBe(false);
  });
});
```

- [ ] **Step 3: 跑测试 + 提交**

Run: `pnpm test src/main/chat/messages.test.ts`
Expected: PASS

Run: `pnpm typecheck`
Expected: 无错误

```bash
git add src/main/chat/messages.ts src/main/chat/messages.test.ts
git commit -m "feat(messages): derive incomplete-turn from trailing user row (#9 P2)

isLastTurnIncomplete：尾消息为 user 行即未完成轮（DD-§3.1 崩溃恢复
读时派生，零持久化）。消费方为 renderer 重试入口（RA 轨）。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Self-Review

**1. Spec 覆盖（对照 §3 / DD-§3.1 / DD-§3.2）：**

- `messages.status` 三态列 + CHECK、出生即终态 → Task 1 Step 2。✅
- `metadata.usage` 真填 → Task 2 Step 2（从 `streamText.onFinish.totalUsage`）。✅
- `metadata.error{name,message}` 透传 → Task 1 Step 1 + Task 2 Step 2。✅
- 落库规则（complete/error/aborted 三终态、中止保留 partial）→ Task 2 Step 2/5。✅
- 仅两个写点、assistant 仅终止时写一次 → 保留 `send.ts:81` user 写、onFinish 单次写 assistant。✅
- DD-§3.1 崩溃派生（尾-user）→ Task 3。✅
- DD-§3.2 区分 error/aborted（`isAborted` 权威）→ Task 2 Step 2 用 `isAborted` 而非嗅探。✅

**2. 占位扫描：** 无 TBD/TODO；每步给出完整代码与精确行号锚点。Task 1 Step 6 的迁移是 `pnpm db:generate` 产物，给出预期重建 SQL 形状与安全性说明，非占位。Task 2 Step 5 的「实施提示」是时序调参指引，非缺失代码。✅

**3. 类型/命名一致性：** `MessageStatus`（types.ts 定义、chat.ts/messages.ts 消费）、`status` 字段（schema 列 / DTO / AppendMessageInput / toDto / insert 一致）、`capturedUsage`/`errorInfo`/`streamHadError`（send.ts 内一致）、`metadata.usage` 扁平 `{inputTokens,outputTokens}`（schema 与 SDK `LanguageModelUsage` 映射一致）、`isLastTurnIncomplete`（定义/测试一致）。✅

**4. 行为正确性：** user 消息不传 `status` → 默认 `complete`（user/system 恒 complete）；assistant 三终态显式传；usage 仅在 `inputTokens`/`outputTokens` 均非空时记录（best-effort，符合「尽量记」）；error 仅 error 终态带（aborted 不带 error 对象，DD-§3.2）。✅
