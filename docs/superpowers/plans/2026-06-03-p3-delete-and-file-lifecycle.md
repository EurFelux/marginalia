# P3 · 删除与文件生命周期簇 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为删书安全落地三件事：①给所有 book-owned 子表加 `ON DELETE CASCADE`；②ePub 文件改为 **app 自有副本**（派生路径、删 `books.path` 列）；③`deleteBook` 服务（DB 级联删 + best-effort 删文件）+ IPC。

**Architecture:** 三阶段、有序：**P3a** schema FK 全加 `onDelete:"cascade"`（声明式，`runMigrations` 事务外切 FK 使表重建安全）；**P3b** 新增 `book-files.ts`（`storedEpubPath`/`writeEpubFile`/`readEpubFile`/`deleteEpubFile`，全注入 `booksDir` → headless 可测），导入复制进 `userData/books/<sha256(bookId)>.epub`，所有读取改派生位置，删 `books.path` 列与 `BookSummaryDto.path`；**P3c** `deleteBook(db, booksDir, bookId)`（先 DB 级联删、再 best-effort unlink，DD-§1.3）+ `library:delete` IPC + preload。

**Tech Stack:** TypeScript 6、Drizzle 1.0-rc（better-sqlite3，`PRAGMA foreign_keys=ON`）、Vitest 4、Electron 41（`app.getPath`）、`node:fs/promises`、`node:crypto`。设计依据：`docs/superpowers/specs/2026-06-03-db-lifecycle-rules-design.md` §1 / DD-§1.1 / DD-§1.2 / DD-§1.3。

**已核事实：**

- `books.path` 的读取方：`book-bytes.ts:readBookBytes`、`send-deps.ts:createLoadBytes`、`library-handlers.ts` 的 chapter-text handler（行 147）、`toDto`（`BookSummaryDto.path`）。
- `BookSummaryDto.path` **renderer 零消费**（grep `src/renderer` 无命中）→ 可随列一并移除。
- `importBook` 是纯函数；`createLoadBytes(db)` 是 glue 工厂；`app.getPath` 仅 Electron 可用 → `booksDir` 必须注入纯函数层。
- `createDb` 设 `PRAGMA foreign_keys=ON`（`client.ts:13`）→ 运行时 CASCADE 生效。

> **⚠ Scope 偏离（需你知晓）：** spec §1.2 列了「存量书惰性回填」。但迁移是 SQL、无法拷贝文件，真正回填需「保留 `path` 列一版 + 读时回填 + 后续版删列」两段式发布。**因项目尚未发布（无真实用户数据）**，本计划 P3b 直接删列；任何遗留的外部路径书在读取时走 `readEpubFile` ENOENT → **缺失态 → 重导(relink)**。若日后有生产用户，需改回两段式回填。

> **⚠ 跨计划重叠：** `repository.ts:importBook` 的 chapter 插入里的 `summaryStatus: "pending"` 由 **P1** 删除；本计划只动同函数的 `path`/`filePath`（不同行，互不冲突）。三计划各开分支时，`pnpm db:generate` 的迁移需按合并顺序生成——分支落后于 main 时 rebase 后重跑 `db:generate`（drizzle 快照按当前 schema 生成）。

---

## File Structure

| 文件                                        | 责任                                                       | Task    |
| ------------------------------------------- | ---------------------------------------------------------- | ------- |
| `src/main/db/schema.ts`                     | 6 个 owned FK 加 `onDelete:"cascade"`                      | P3a     |
| `src/main/library/repository.test.ts`       | 级联测试 + 删书测试 + 去 path/filePath                     | P3a,b,c |
| `src/main/library/book-files.ts`（新）      | 派生路径 + 读/写/删 app 自有副本（注入 booksDir）          | P3b     |
| `src/main/library/book-files.test.ts`（新） | book-files 单测（temp dir）                                | P3b     |
| `src/main/db/instance.ts`                   | `getBooksDir()` glue（Electron）                           | P3b     |
| `src/main/ai/send-deps.ts`                  | `createLoadBytes(booksDir)` 改注入                         | P3b     |
| `src/main/library/book-bytes.ts`            | 删除（并入 book-files）                                    | P3b     |
| `src/main/library/repository.ts`            | `importBook` 去 path/filePath；加 `deleteBook`             | P3b,c   |
| `src/main/ipc/library-handlers.ts`          | import 复制文件、读派生位置、toDto 去 path、delete handler | P3b,c   |
| `src/shared/library.ts`                     | `BookSummaryDto` 去 `path`                                 | P3b     |
| `src/shared/ipc.ts`                         | 加 `libraryDelete` 通道                                    | P3c     |
| `src/preload.ts`                            | `window.api.library.delete`                                | P3c     |
| `src/main/db/migrations/<new>/`             | 级联迁移、删 path 迁移                                     | P3a,b   |

---

## Task P3a: FK 全加 ON DELETE CASCADE

**Files:**

- Modify: `src/main/db/schema.ts`
- Test: `src/main/library/repository.test.ts`
- Create: `src/main/db/migrations/<auto-generated>/`

- [ ] **Step 1: `schema.ts` 给 6 个 owned FK 加 `onDelete:"cascade"`**

逐一修改 `src/main/db/schema.ts` 的 `.references(...)`：

`chapters.bookId`（当前第 71–73 行）：

```ts
    bookId: text("book_id")
      .notNull()
      .references(() => books.id, { onDelete: "cascade" }),
```

`progress.bookId`（当前第 95–97 行）：

```ts
  bookId: text("book_id")
    .primaryKey()
    .references(() => books.id, { onDelete: "cascade" }),
```

`annotations.bookId`（当前第 108–110 行）：

```ts
    bookId: text("book_id")
      .notNull()
      .references(() => books.id, { onDelete: "cascade" }),
```

`conversations.bookId`（当前第 133–135 行）：

```ts
    bookId: text("book_id")
      .notNull()
      .references(() => books.id, { onDelete: "cascade" }),
```

`conversations.chapterId`（当前第 136 行）：

```ts
    chapterId: text("chapter_id").references(() => chapters.id, { onDelete: "cascade" }), // NULL = 独立会话
```

`messages.conversationId`（当前第 153–155 行）：

```ts
    conversationId: text("conversation_id")
      .notNull()
      .references(() => conversations.id, { onDelete: "cascade" }),
```

**不动**：`conversations.assistantId → assistants`（共享资源）、`assistants.providerId → providers`。

- [ ] **Step 2: `repository.test.ts` 加级联测试**

`src/main/library/repository.test.ts` 导入（当前第 4–6 行）扩展：

```ts
import { eq } from "drizzle-orm";
import {
  annotations,
  assistants,
  books,
  chapters,
  conversations,
  messages,
  progress,
} from "@main/db/schema";
import { getBook, importBook, listBooks, resolveChapterByHref } from "@main/library/repository";
import { makeFixtureEpub } from "@marginalia/epub-parser";
```

在 `describe("library repository", …)` 内末尾（最后一个用例 `});` 之后、describe 的 `});` 之前）加：

```ts
it("ON DELETE CASCADE removes all book-owned dependents", () => {
  const db = freshDb();
  const book = importBook(db, { bytes: makeFixtureEpub(), filePath: "/b.epub" });
  const ch1 = resolveChapterByHref(db, book.id, "OEBPS/ch1.xhtml")!;
  const assistantId = db.insert(assistants).values({ name: "A" }).returning().get().id;
  db.insert(progress).values({ bookId: book.id, cfi: "epubcfi(/6/2)" }).run();
  db.insert(annotations)
    .values({ bookId: book.id, style: "yellow", selectedText: "x", cfiRange: "r" })
    .run();
  const conv = db
    .insert(conversations)
    .values({ bookId: book.id, chapterId: ch1.id, assistantId })
    .returning()
    .get();
  db.insert(messages).values({ conversationId: conv.id, role: "user", parts: [], seq: 0 }).run();

  db.delete(books).where(eq(books.id, book.id)).run();

  expect(listBooks(db)).toHaveLength(0);
  expect(db.select().from(chapters).all()).toHaveLength(0);
  expect(db.select().from(progress).all()).toHaveLength(0);
  expect(db.select().from(annotations).all()).toHaveLength(0);
  expect(db.select().from(conversations).all()).toHaveLength(0);
  expect(db.select().from(messages).all()).toHaveLength(0);
  // assistant 是共享资源，不随书删
  expect(db.select().from(assistants).all()).toHaveLength(1);
});
```

- [ ] **Step 3: 生成级联迁移**

Run: `pnpm db:generate`
Expected: 新增迁移子目录。FK 变更触发 `chapters`/`progress`/`annotations`/`conversations`/`messages` 的**表重建**（带 `ON DELETE CASCADE` 重建 FK）。`runMigrations` 事务外 `PRAGMA foreign_keys=OFF` 使重建期的 `DROP TABLE` 不撞既有子表引用（记忆 `drizzle-migrate-fk-transaction-gotcha`）。

- [ ] **Step 4: 跑测试 + 提交**

Run: `pnpm test src/main/library/repository.test.ts`
Expected: PASS（级联测试 + 原有用例全绿；`PRAGMA foreign_keys=ON` 由 `createDb` 设，CASCADE 运行时生效）

Run: `pnpm typecheck`
Expected: 无错误

```bash
git add src/main/db/schema.ts src/main/library/repository.test.ts src/main/db/migrations/
git commit -m "feat(db): add ON DELETE CASCADE to book-owned foreign keys (#9 P3a)

chapters/progress/annotations/conversations(→books, +chapterId→chapters)
/messages(→conversations) 全加 onDelete:cascade；assistants 共享不级联。
表重建迁移。为删书铺路。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task P3b: 文件自有化（派生路径 + 删 books.path）

**Files:**

- Create: `src/main/library/book-files.ts`
- Create: `src/main/library/book-files.test.ts`
- Modify: `src/main/db/instance.ts`
- Modify: `src/main/ai/send-deps.ts`
- Delete: `src/main/library/book-bytes.ts`
- Modify: `src/main/library/repository.ts`
- Modify: `src/main/ipc/library-handlers.ts`
- Modify: `src/shared/library.ts`
- Modify: `src/main/library/repository.test.ts`
- Create: `src/main/db/migrations/<auto-generated>/`

- [ ] **Step 1: 新建 `book-files.ts`（派生路径 + 读/写/删，注入 booksDir）**

Create `src/main/library/book-files.ts`:

```ts
import { createHash } from "node:crypto";
import { mkdir, readFile, unlink, writeFile } from "node:fs/promises";
import path from "node:path";

/** ePub 文件缺失（app 自有副本不在派生位置）——供派生 missing 态 / relink 提示。 */
export class EpubFileMissingError extends Error {
  constructor(public readonly bookId: string) {
    super(`epub file missing for book ${bookId}`);
    this.name = "EpubFileMissingError";
  }
}

/**
 * app 自有 ePub 副本的**派生**路径：`booksDir/<sha256(bookId)>.epub`。
 * 不入库（位置由 bookId 确定性派生，符合 DB lifecycle spec §0 / DD-§1.2）。
 * 用 sha256 哈希编码——bookId 可能是 ePub UID（`urn:uuid:…`/`http://…`，含 `:`/`/` 文件系统非法字符）。
 * 编码函数须**永久稳定**（改了旧文件即失联）。
 */
export function storedEpubPath(booksDir: string, bookId: string): string {
  const name = createHash("sha256").update(bookId).digest("hex");
  return path.join(booksDir, `${name}.epub`);
}

/** 复制 ePub 字节进 app 自有位置（覆盖写；relink/重导即重写）。 */
export async function writeEpubFile(
  booksDir: string,
  bookId: string,
  bytes: Uint8Array,
): Promise<void> {
  await mkdir(booksDir, { recursive: true });
  await writeFile(storedEpubPath(booksDir, bookId), bytes);
}

/** 读 app 自有副本；缺失抛 EpubFileMissingError。 */
export async function readEpubFile(booksDir: string, bookId: string): Promise<Uint8Array> {
  try {
    return new Uint8Array(await readFile(storedEpubPath(booksDir, bookId)));
  } catch (err) {
    if ((err as NodeJS.ErrnoException).code === "ENOENT") throw new EpubFileMissingError(bookId);
    throw err;
  }
}

/** best-effort 删除自有副本（删书时调；缺失无害，仅记日志）。 */
export async function deleteEpubFile(booksDir: string, bookId: string): Promise<void> {
  await unlink(storedEpubPath(booksDir, bookId)).catch((err: NodeJS.ErrnoException) => {
    if (err.code !== "ENOENT") console.warn(`[book-files] unlink ${bookId} failed:`, err);
  });
}
```

- [ ] **Step 2: `book-files.test.ts` 单测（temp dir）**

Create `src/main/library/book-files.test.ts`:

```ts
import { mkdtemp, readFile, rm } from "node:fs/promises";
import { tmpdir } from "node:os";
import path from "node:path";
import { afterEach, beforeEach, describe, expect, it } from "vitest";
import {
  EpubFileMissingError,
  deleteEpubFile,
  readEpubFile,
  storedEpubPath,
  writeEpubFile,
} from "@main/library/book-files";

describe("book-files", () => {
  let dir: string;
  beforeEach(async () => {
    dir = await mkdtemp(path.join(tmpdir(), "marginalia-books-"));
  });
  afterEach(async () => {
    await rm(dir, { recursive: true, force: true });
  });

  it("derives a filesystem-safe path from an unsafe bookId (urn/url)", () => {
    const p = storedEpubPath(dir, "urn:uuid:abc/def");
    expect(p.startsWith(dir)).toBe(true);
    expect(path.basename(p)).toMatch(/^[0-9a-f]{64}\.epub$/); // 哈希文件名，无 : 或 /
  });

  it("write then read round-trips the bytes", async () => {
    const bytes = new Uint8Array([1, 2, 3]);
    await writeEpubFile(dir, "book-1", bytes);
    expect(await readEpubFile(dir, "book-1")).toEqual(bytes);
    // 真落到派生位置
    expect(new Uint8Array(await readFile(storedEpubPath(dir, "book-1")))).toEqual(bytes);
  });

  it("read throws EpubFileMissingError when the copy is absent", async () => {
    await expect(readEpubFile(dir, "missing")).rejects.toBeInstanceOf(EpubFileMissingError);
  });

  it("delete is best-effort (no throw when already absent)", async () => {
    await expect(deleteEpubFile(dir, "never-written")).resolves.toBeUndefined();
  });
});
```

- [ ] **Step 3: `instance.ts` 加 `getBooksDir()` glue**

`src/main/db/instance.ts` 在 `getDb` 之后加（`path`/`app` 已 import）：

```ts
/** app 自有 ePub 副本根目录（Electron glue）。纯函数层通过此值注入 booksDir。 */
export function getBooksDir(): string {
  return path.join(app.getPath("userData"), "books");
}
```

- [ ] **Step 4: `send-deps.ts` 的 `createLoadBytes` 改注入 booksDir**

`src/main/ai/send-deps.ts` 顶部 import 调整（去 `readFile`/`getBook`，加 book-files + getBooksDir）：

```ts
import { getBooksDir, getDb } from "@main/db/instance";
import { readEpubFile } from "@main/library/book-files";
import { safeStorageEncryptor } from "@main/secrets/safe-storage-encryptor";
import { resolveAssistantModel } from "@main/ai/assistant-model";
import type { SummaryDeps } from "@main/ai/summary";
import type { LoadBytes } from "@main/ai/tools";
import type { SendDeps } from "@main/ai/send";
```

`createLoadBytes`（当前第 11–19 行）替换为（注入 booksDir，从派生位置读）：

```ts
/** (bookId) => 该书 app 自有 ePub 副本字节；缺失抛 EpubFileMissingError。注入 booksDir 以便单测。 */
export function createLoadBytes(booksDir: string): LoadBytes {
  return (bookId: string) => readEpubFile(booksDir, bookId);
}
```

`makeSendDeps`/`makeSummaryDeps`（当前第 22–37 行）里 `createLoadBytes(db)` → `createLoadBytes(getBooksDir())`：

```ts
export function makeSendDeps(): SendDeps {
  const db = getDb();
  const loadBytes = createLoadBytes(getBooksDir());
  const resolveModel = () => resolveAssistantModel(db, safeStorageEncryptor);
  return { db, loadBytes, resolveModel };
}

export function makeSummaryDeps(): SummaryDeps {
  const db = getDb();
  return {
    db,
    loadBytes: createLoadBytes(getBooksDir()),
    resolveModel: () => resolveAssistantModel(db, safeStorageEncryptor),
  };
}
```

- [ ] **Step 5: 删除 `book-bytes.ts`**

Delete `src/main/library/book-bytes.ts`（其唯一消费方 readEpubBytes handler 在 Step 7 改用 `readEpubFile`）。

- [ ] **Step 6: `repository.ts` 的 `importBook` 去 path/filePath**

`src/main/library/repository.ts`：`ImportInput`（当前第 7–10 行）去 `filePath`：

```ts
export interface ImportInput {
  bytes: Uint8Array;
}
```

`importBook` 的 `books` 插入（当前第 32–41 行）去 `path: input.filePath,` 一行：

```ts
tx.insert(books)
  .values({
    id,
    title: parsed.title ?? null,
    author: parsed.author ?? null,
    cover: parsed.cover ? Buffer.from(parsed.cover) : null,
    toc: parsed.toc,
  })
  .run();
```

（chapter 插入里的 `summaryStatus` 由 P1 处理，本步不动。）

- [ ] **Step 7: `library-handlers.ts` — import 复制文件、读派生位置、toDto 去 path**

`src/main/ipc/library-handlers.ts`：

import 区（当前第 17–19 行附近）加 `getBooksDir`、`readEpubFile`、`writeEpubFile`，去 `readBookBytes`：

```ts
import { getBooksDir, getDb } from "@main/db/instance";
import { getBook, importBook, listBooks } from "@main/library/repository";
import { readEpubFile, writeEpubFile } from "@main/library/book-files";
```

`toDto`（当前第 32–42 行）去 `path`：

```ts
const toDto = (b: { id: string; title: string | null; author: string | null }): BookSummaryDto => ({
  id: b.id,
  title: b.title,
  author: b.author,
});
```

import handler（当前第 45–55 行）改为导入后复制字节进派生位置：

```ts
handle<{ filePath: string }, BookSummaryDto>(IPC.libraryImport, importBookInput, async (input) => {
  const buf = await readFile(input.filePath).catch((err: NodeJS.ErrnoException) => {
    throw new Error(`Cannot read epub file at "${input.filePath}": ${err.code ?? err.message}`);
  });
  const bytes = new Uint8Array(buf);
  const book = importBook(getDb(), { bytes });
  await writeEpubFile(getBooksDir(), book.id, bytes); // 复制进 app 自有位置（relink/重导即覆盖）
  return toDto(book);
});
```

readEpubBytes handler（当前第 74–76 行）改读派生位置：

```ts
handle<{ bookId: string }, Uint8Array>(IPC.libraryReadEpubBytes, bookIdInput, (input) =>
  readEpubFile(getBooksDir(), input.bookId),
);
```

chapter-text handler 的 `readFile(book.path)`（当前第 147–152 行）改为：

```ts
const bytes = await readEpubFile(getBooksDir(), input.bookId).catch((err) => {
  throw new Error(
    `Cannot read epub for book "${input.bookId}": ${err instanceof Error ? err.message : String(err)}. The file may be missing — re-import the book.`,
  );
});
```

（`book` 仍由上方 `getBook` 取用于存在性校验；只是不再读 `book.path`。`readFile` 若已无其他用处可从 import 移除。）

- [ ] **Step 8: `shared/library.ts` 去 `BookSummaryDto.path`**

先确认零消费：

Run: `git grep -n "\.path" -- src/renderer src/preload.ts | grep -i book`
Expected: 无命中（renderer 不消费 `BookSummaryDto.path`）

`src/shared/library.ts` 的 `BookSummaryDto`（当前第 24–29 行）去 `path`：

```ts
export interface BookSummaryDto {
  id: string;
  title: string | null;
  author: string | null;
}
```

- [ ] **Step 9: `repository.test.ts` 去 path/filePath**

所有 `importBook(db, { bytes: …, filePath: … })` → `importBook(db, { bytes: … })`（当前第 18、37–40、49、54、65 行及 P3a Step 2 新增的级联用例）。
删除 path 断言（当前第 21 行）：

```ts
expect(getBook(db, book.id)?.path).toBe("/books/fixture.epub");
```

- [ ] **Step 10: 生成删列迁移**

Run: `pnpm db:generate`
Expected: 新增迁移子目录——`books` 表重建（去 `path` 列、保留 `summary`/`cover`/`toc` 等）。`books` 被多子表 FK 引用，表重建的 `DROP TABLE books` 由 `runMigrations` 事务外 FK-off 保护。

- [ ] **Step 11: 跑测试 + 类型检查 + 提交**

Run: `pnpm test src/main/library/book-files.test.ts src/main/library/repository.test.ts`
Expected: PASS

Run: `pnpm typecheck`
Expected: 无错误（`books.path`/`book-bytes.ts`/`BookSummaryDto.path` 全消除；`git grep -n "books.path\|book-bytes\|\.path" src/main` 应无残留引用旧列）

```bash
git add src/main/library/book-files.ts src/main/library/book-files.test.ts src/main/db/instance.ts src/main/ai/send-deps.ts src/main/library/repository.ts src/main/ipc/library-handlers.ts src/shared/library.ts src/main/library/repository.test.ts src/main/db/migrations/
git rm src/main/library/book-bytes.ts
git commit -m "feat(library): own imported epub files at derived path, drop books.path (#9 P3b)

新增 book-files.ts（storedEpubPath/write/read/delete，注入 booksDir）；
导入复制进 userData/books/<sha256(bookId)>.epub，读取走派生位置；
删 books.path 列与 BookSummaryDto.path。缺失文件抛 EpubFileMissingError。
（pre-release：不做两段式回填，遗留外部路径书走缺失→重导。）

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task P3c: `deleteBook` 服务 + IPC

**Files:**

- Modify: `src/main/library/repository.ts`
- Modify: `src/shared/ipc.ts`
- Modify: `src/main/ipc/library-handlers.ts`
- Modify: `src/preload.ts`
- Test: `src/main/library/repository.test.ts`

- [ ] **Step 1: `repository.ts` 加 `deleteBook`**

`src/main/library/repository.ts` 末尾加（DD-§1.3：先 DB 级联删、再 best-effort 删文件）：

```ts
import { deleteEpubFile } from "@main/library/book-files"; // 顶部 import 区

/**
 * 删书：先删 DB 行（真相源；依赖行靠 FK ON DELETE CASCADE 自动清，P3a），再 best-effort 删自有副本文件。
 * 顺序不可反——指向已删文件的 DB 行 = 打不开的鬼书，比无主文件（可 GC）更糟（DD-§1.3）。
 */
export async function deleteBook(db: DB, booksDir: string, bookId: string): Promise<void> {
  db.delete(books).where(eq(books.id, bookId)).run();
  await deleteEpubFile(booksDir, bookId);
}
```

- [ ] **Step 2: `shared/ipc.ts` 加 `libraryDelete` 通道**

`src/shared/ipc.ts` 在 `libraryReadEpubBytes`（当前第 11 行）后加：

```ts
  libraryDelete: "library:delete",
```

- [ ] **Step 3: `library-handlers.ts` 加 delete handler**

`src/main/ipc/library-handlers.ts`：import 区加 `deleteBook`（与 `importBook` 同行）：

```ts
import { deleteBook, getBook, importBook, listBooks } from "@main/library/repository";
```

在 readEpubBytes handler 后加：

```ts
handle<{ bookId: string }, void>(IPC.libraryDelete, bookIdInput, (input) =>
  deleteBook(getDb(), getBooksDir(), input.bookId),
);
```

- [ ] **Step 4: `preload.ts` 加 `library.delete`**

`src/preload.ts` 的 `library` 对象（readEpubBytes 之后）加：

```ts
    delete: (input: BookIdInput): Promise<void> => ipcRenderer.invoke(IPC.libraryDelete, input),
```

（`BookIdInput` 已在该文件 import；`delete` 是合法对象方法名。）

- [ ] **Step 5: `repository.test.ts` 加 `deleteBook` 测试**

`src/main/library/repository.test.ts`：import 加 `deleteBook`、加 node fs/os/path（用 temp booksDir 验文件删除）：

```ts
import { mkdtemp, readFile, rm, writeFile } from "node:fs/promises";
import { tmpdir } from "node:os";
import {
  deleteBook,
  getBook,
  importBook,
  listBooks,
  resolveChapterByHref,
} from "@main/library/repository";
import { storedEpubPath } from "@main/library/book-files";
```

加用例：

```ts
it("deleteBook removes the book (cascading dependents) and unlinks the owned file", async () => {
  const db = freshDb();
  const booksDir = await mkdtemp(path.join(tmpdir(), "marginalia-del-"));
  try {
    const book = importBook(db, { bytes: makeFixtureEpub() });
    await writeFile(storedEpubPath(booksDir, book.id), new Uint8Array([1]));

    await deleteBook(db, booksDir, book.id);

    expect(listBooks(db)).toHaveLength(0);
    expect(db.select().from(chapters).all()).toHaveLength(0); // 级联
    await expect(readFile(storedEpubPath(booksDir, book.id))).rejects.toMatchObject({
      code: "ENOENT",
    }); // 文件已删
  } finally {
    await rm(booksDir, { recursive: true, force: true });
  }
});

it("deleteBook tolerates an already-missing file (best-effort unlink)", async () => {
  const db = freshDb();
  const booksDir = await mkdtemp(path.join(tmpdir(), "marginalia-del-"));
  try {
    const book = importBook(db, { bytes: makeFixtureEpub() }); // 未写文件
    await expect(deleteBook(db, booksDir, book.id)).resolves.toBeUndefined();
    expect(listBooks(db)).toHaveLength(0);
  } finally {
    await rm(booksDir, { recursive: true, force: true });
  }
});
```

- [ ] **Step 6: 跑测试 + 类型检查 + 提交**

Run: `pnpm test src/main/library/repository.test.ts`
Expected: PASS

Run: `pnpm typecheck`
Expected: 无错误

```bash
git add src/main/library/repository.ts src/shared/ipc.ts src/main/ipc/library-handlers.ts src/preload.ts src/main/library/repository.test.ts
git commit -m "feat(library): add deleteBook service + library:delete IPC (#9 P3c)

deleteBook：先 DB 级联删（P3a CASCADE）、再 best-effort unlink 自有副本
（DD-§1.3 DB 先文件后）。新增 library:delete 通道 + preload。删书 UI 属 RA 轨。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Self-Review

**1. Spec 覆盖（对照 §1 / DD-§1.1/1.2/1.3）：**

- owned 子表全 `ON DELETE CASCADE`、assistant 不级联 → P3a。✅
- 文件复制进 `userData/books/<sha256(bookId)>.epub`、删 `books.path`、派生路径 → P3b。✅
- 文件名安全（sha256 编码 unsafe bookId）→ P3b Step 1 + test。✅
- missing 派生（`EpubFileMissingError`）+ relink/重导 → P3b（缺失抛错；重导即 import 覆盖）。✅
- 删书 DB 先文件后、best-effort unlink → P3c。✅
- **偏离**：spec 的「惰性回填」因 pre-release 简化为「缺失→重导」（顶部 Scope 偏离已显式记录）。⚠

**2. 占位扫描：** 无 TBD/TODO；每步完整代码 + 行号锚点。两处 `pnpm db:generate` 为产物，给出预期重建 SQL 与安全说明。✅

**3. 类型/命名一致性：** `storedEpubPath`/`writeEpubFile`/`readEpubFile`/`deleteEpubFile`/`EpubFileMissingError`（book-files.ts 定义、handlers/send-deps/repository/测试消费一致）、`getBooksDir`（instance.ts 定义、handlers/send-deps 消费）、`deleteBook(db, booksDir, bookId)`（定义/handler/测试一致）、`ImportInput { bytes }`（去 filePath 后 importBook/handlers/测试一致）、`BookSummaryDto` 去 `path`（DTO/toDto 一致）。✅

**4. 跨阶段/跨计划一致性：** P3a 测试用 `{ bytes, filePath }`（彼时 importBook 未改签名），P3b Step 9 统一改为 `{ bytes }`——同分支 a→b→c 顺序执行无冲突。与 P1 仅在 `importBook` 不同行（summaryStatus vs path）相交，互不冲突。迁移按合并顺序生成（顶部已注明 rebase 后重跑 db:generate）。✅
